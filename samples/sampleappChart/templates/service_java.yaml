apiVersion: v1
kind: Service
metadata:
  name: {{ .Values.app.java.name }}
  {{- template "common.metadata" . }}
spec:
  type: LoadBalancer
  ports:
    - port: 80
      protocol: TCP
      targetPort: 8080
  selector:
    app: {{ .Values.app.java.name }}
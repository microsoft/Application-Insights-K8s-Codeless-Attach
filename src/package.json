{
  "name": "mutating-webhook",
  "version": "0.0.0",
  "description": "MutatingWebhook",
  "license": "MIT",
  "repository": "https://github.com/microsoft/Application-Insights-K8s-Codeless-Attach/",
  "scripts": {
    "build": "tsc --build",
    "clean": "tsc --build --clean",
    "start": "node server",
    "test": "node ContentProcessorTests",
    "lint": "tslint --fix -c tslint.json *.ts"
  },
  "author": {
    "name": ""
  },
  "dependencies": {
    "@kubernetes/client-node": "^0.11.0",
    "@types/node": "^8.0.14",
    "applicationinsights": "^1.4.2",
    "log4js": "5.1.0",
    "tslint": "^5.20.0"
  },
  "engines": {
    "node": "^8.0.0"
  },
  "devDependencies": {
    "typescript": "^3.7.2"
  }
}
